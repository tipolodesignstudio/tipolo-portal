// Local config — fill in from your Supabase project (see config.example.js).
// This file is intentionally NOT git-ignored: the anon key is safe to commit and is
// required for the deployed site to work. Never add the service_role key here.

export const SUPABASE_URL = "https://YOUR-PROJECT-ref.supabase.co";
export const SUPABASE_ANON_KEY = "YOUR-ANON-PUBLIC-KEY";

export const ALLOWED_EMAIL_DOMAIN = "tipolo.ca";
